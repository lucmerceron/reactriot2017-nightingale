import React from 'react'
import PropTypes from 'prop-types'

import './Button.css'

const Button = props => {
  return (
    <div>
      
    </div>
  )
}

Button.propTypes = {
  
}

export default Button